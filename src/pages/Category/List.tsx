import * as React from 'react';
import { Page } from '../../components/Page';

type ListProps = {};

const List: React.FC = (props: ListProps) => (
  <Page title="Listar categorias">Listar categorias</Page>
);

export default List;
