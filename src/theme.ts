import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#79aec8',
      contrastText: '#000000',
    },
    secondary: {
      main: '#4db5ab',
      contrastText: '#ffffff',
    },
  },
});

export default theme;
